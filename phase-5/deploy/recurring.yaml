apiVersion: apps/v1
kind: Deployment
metadata:
  name: recurring-service
  labels:
    app: recurring-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: recurring-service
  template:
    metadata:
      labels:
        app: recurring-service
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "recurring-service"
        dapr.io/app-port: "8000"
    spec:
      containers:
        - name: recurring-service
          image: recurring-service:phase5
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 8000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: recurring-service
spec:
  selector:
    app: recurring-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
